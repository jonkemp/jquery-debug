{
  "name": "jquery-debug",
  "version": "1.0.0",
  "description": "A plugin that makes jQuery throw an error, rather than fail silently, when a selector returns no results.",
  "main": "jquery-debug.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "jquery-debug.js"
  ],
  "repository": "jonkemp/jquery-debug",
  "keywords": [
    "jquery-plugin",
    "ecosystem:jquery",
    "jquery",
    "debug"
  ],
  "author": "Jonathan Kemp <kempdogg@gmail.com> (http://jonkemp.com/)",
  "license": "MIT"
}
